## Index Block Script

### Description
This script takes a CSV as input. The CSV contains the assets that have sitemap metadata of "No" and sets it to "Yes".
### How to use

To set up your environment, copy [.env.example](.env.example) to `.env` and fill in the required values.

